<!DOCTYPE html>
<html lang="en">
{{> head}}
  <body>
    <header>
       {{> nav}}
      <div class="logo">
        <h2 class="title-tech"><a href="#"> Your Dashboard</a> </h2>
      </div>
      </header>
<div class="align-grid">
<div class="grid-container">
        {{{body}}}
</div>
</div>
    {{#if logged_in}}
      <script src='/js/logout.js'></script>
    {{/if}}
   
  </body>
</html>